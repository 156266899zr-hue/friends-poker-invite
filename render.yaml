services:
  - type: web
    name: friends-poker-invite
    runtime: node
    plan: free
    buildCommand: node -e "console.log('No build step required')"
    startCommand: npm start
    healthCheckPath: /
